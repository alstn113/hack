const Loading = () => {
  return (
    <main className="container mx-auto flex flex-col items-center justify-center h-screen">
      <h2 className="text-3xl text-primary font-bold mb-4">Loading...</h2>
      <p className="text-gray-600">Hopefully not for too long :)</p>
    </main>
  );
};

export default Loading;
